# AGENTS.md

This repo follows global agent policies:
- `~/.codex/AGENTS.md`
- `~/.claude/CLAUDE.md`

## Repo Guidance
- Use `$arc-ideabrowser-openclaw-flow` when updating idea extraction context.
- Use `$arc-implementation-plan-progress` when changing scope, and keep
  `IMPLEMENTATION_PLAN.md` + `progress.txt` synchronized.
- Use `$arc-project-deploy-portfolio-sync` only when explicitly asked to deploy and update portfolio listings.
